On the Estimates page, in the "Completed Without Approval (Work Order)" container, fix the photos not displaying in the convert to estimate form:

**THE ISSUE:**
- The table shows "📷 2" indicating 2 photos are attached to each work order
- But when clicking "Convert to Estimate", the Photos section is empty
- The sample photos are not actually being stored or displayed

**FIX:**

1. **Add actual sample images to the database/storage for each work order:**
   - Generate or use placeholder pool equipment images
   - Store Before and After photos for each work order
   - Link the photos to the work order records

2. **Display photos in the "Convert to Estimate" form:**
   - In the "Photos" section, show the attached images as thumbnails
   - Label each photo as "Before" or "After"
   - Show captions below each photo

3. **Photos section should show:**
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│ 📷 Photos                                                      [+ Add Photos]  │
├─────────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────┐  ┌─────────────────┐                                       │
│ │                 │  │                 │                                       │
│ │  [Before Image] │  │  [After Image]  │                                       │
│ │                 │  │                 │                                       │
│ └─────────────────┘  └─────────────────┘                                       │
│  Before: Corroded     After: New 1.5HP                                         │
│  motor                motor installed                                           │
└─────────────────────────────────────────────────────────────────────────────────┘
```

4. **Use placeholder images if needed:**
   - Use stock photos of pool equipment
   - Or generate placeholder images with labels "Before Photo" / "After Photo"
   - Ensure images are actually rendered, not just indicated

5. **Make sure photos transfer when converting:**
   - When "Convert to Estimate" is clicked, photos should carry over to the new estimate
   - Photos should be visible in the estimate details after saving